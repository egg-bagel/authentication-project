<h1>Posts Page</h1>

<% @posts.each do |post| %>
  <% if user_signed_in? %>
    <%= "Author: #{post.user.username}" %>
  <% end %>
  <h3><%= post.title %></h3>
  <p><%= post.body %></p>
  <p>===================================================================</p>
<% end %>